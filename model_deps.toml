# Model-specific pip dependencies.
# Keys are lowercase patterns matched against model names.
# Values are lists of pip packages to install.

[kokoro]
deps = ["misaki", "num2words", "spacy"]

[qwen3-tts]
deps = ["misaki"]

[csm]
deps = ["tokenizers"]
